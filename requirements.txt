# Plate Status Detection - pinned dependencies for the GPU container.
#
# NOTE on torch/torchvision: pin these to versions that match the container's CUDA.
# The two lines below target CUDA 12.1 wheels; if the container image already ships a
# CUDA build of torch, you can drop these two lines and keep the rest.
torch==2.3.1
torchvision==0.18.1

# --- Step 02: image generation (FLUX.1-dev via diffusers) ---
diffusers==0.31.0
transformers==4.44.2
accelerate==0.34.2
huggingface_hub==0.25.1
sentencepiece==0.2.0
protobuf==5.28.2

# --- Step 05/06: training + metrics ---
scikit-learn==1.5.1

# --- Experiment tracking ---
wandb==0.17.9

# --- Headless notebook execution + plotting + utils ---
jupyter==1.1.1
nbconvert==7.16.4
ipykernel==6.29.5
matplotlib==3.9.2
numpy==1.26.4
pillow==10.4.0
tqdm==4.66.5
